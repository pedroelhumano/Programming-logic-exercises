#include <stdio.h>
#include <string.h>

// Funcion strlen usando la biblioteca standard

int main()
{
  char cadena[] = "Hola, mundo!!";
  int longitud = strlen(cadena);
  printf("La longitud de la cadena es: %d\n", longitud);
  return 0;
}
